import UIKit

var str = "变量和常量"

/// 从定义一个变量开始

var Name = "Value" /// String
var hour = 24   /// Integer
var PI = 3.14 /// Double

/// Bool
var swiftIsFun = true


/// 打包多个类型的Tuple Pack many types of values together
var me = ("Mars", 11, "493059732@qq.com")
me.0
me.1
me.2
//me.4 /// 超过范围会报错

/// 可以x这样修改起的值
me.0 = "wxb"
me

/// 常量是不能被修改的变量 关键字let
let minutes = 30
let fireIsHot = true

/// 一旦被定义就无法修改

/// Type inference(推理) / annotation（注释） 类型推断

/// 显式指定 类型
var x: Int = 2
var s: String = "0"






